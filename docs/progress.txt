# Polet AI — Progress

## Week 1
- [x] #1 — Wallet PDA + owner + policy hash storage
- [x] #2 — Execute with allow/block policy enforcement
- [x] #3 — On-chain rate limiting (daily spend tracking)
- [x] #4 — Temporal key grant
- [x] #5 — Session revoke (kill switch)
- [x] #6 — Off-chain proxy: intent parsing + routing

## Week 2
- [x] #7 — Off-chain proxy: transaction construction
- [x] #8 — SDK: intent JSON builder
- [ ] #9 — Policy UI: template-based configuration
- [ ] #10 — Frontend: wallet + temporal key flow
- [ ] #11 — End-to-end BLOCKED demo (HITL)

## Done
- #2 — Allow/block policy enforcement (commit now)
  - Policy struct: allowlist (Vec<Pubkey>) and blocklist (Vec<Pubkey>)
  - set_policy_data instruction: stores serialized Policy in wallet
  - enforce_policy helper: checks destination against allow/block lists
  - Blocklist takes precedence over allowlist
  - Tests: test_policy_allowlist_transfer_succeeds, test_policy_allowlist_transfer_blocked, test_policy_blocklist_transfer_blocked, test_policy_blocklist_precedence
- #1 — Wallet PDA + owner + policy hash storage (commit 7a62842)
  - Wallet account struct: owner, policy_hash, daily_spent, last_reset, daily_limit
  - initialize instruction: creates PDA wallet via seeds [WALLET_SEED, owner]
  - set_policy instruction: owner-only policy hash update
  - ErrorCode enum: NotOwner, PolicyViolation, DailyLimitExceeded, AmountLimitExceeded, etc.
- #5 — Session revoke (kill switch) — implemented in commit e4ae33f (same as #4)
  - revoke_session instruction: sets temporal key `authorized = false`
  - Immediately blocks session key from executing intents
  - Test: test_revoke_session
- #4 — Temporal key grant (commit e4ae33f)
  - TemporalKey struct: key, expires_at, authorized, daily_limit, daily_spent, last_reset
  - grant_temporal_key instruction: owner grants temporal key to session key
  - execute_intent_as_session: session key executes transfer within daily limit
  - Tests: test_grant_temporal_key, test_execute_intent_as_session
- #3 — On-chain rate limiting (commit earlier, confirmed in lib.rs)
  - execute_intent resets daily_spent if new day passed (lines 118-122)
  - Checks amount against daily_limit before executing (lines 124-128)
  - Updates daily_spent after successful transfer (line 142)
  - Test: test_execute_intent_daily_limit_exceeded
- #6 — Off-chain proxy: intent parsing + routing (commit 40bc7a7)
  - Bun + Hono server with CORS for AI agent SDK
  - Intent parser: validates transfer/swap/stake/custom actions
  - Policy engine: evaluates against allow/block lists, amount limits
  - Transaction builder: builds Solana instruction data
  - Mock wallet store for demo
  - 21 passing tests (TDD approach)
  - Routes: POST /intent/evaluate, /intent/execute, /intent/route
- #7 — Off-chain proxy: transaction construction (commit 280bd5a)
  - Real transaction building with @solana/web3.js (Transaction, PublicKey, SystemProgram)
  - buildExecuteIntentData() creates instruction data matching on-chain format
  - buildTransaction() for owner-signed, buildSessionTransaction() for session-signed
  - buildIntentTransaction() high-level API from Intent object
  - to_LE_Bytes() utility for bigint-to-little-endian conversion
  - 4 new tests, 29 passing total
- #8 — SDK: intent JSON builder (commit now)
  - @polet-ai/sdk npm package for AI agent integration
  - createTransferIntent(), createSwapIntent(), createStakeIntent(), createCustomIntent()
  - createUnstakeIntent(), createDelegateIntent(), createUndelegateIntent()
  - isValidIntent() validation function
  - generateIntentId() for unique intent IDs
  - getIntentAmount(), getIntentDestination() helper functions
  - Session management utilities: deriveWalletPDA, formatPublicKey, estimateFee
  - parseIntentParams() for type-safe param parsing
  - 43 passing tests (16 intent builder + 27 session utils)