# Polet AI — Progress

## Status Summary

Polet AI is being redirected from a public policy smart wallet into a confidential Solana control layer for AI agents.

The old v1 work remains useful as prior art, but the final hackathon path may substantially refactor the contract, proxy, SDK, and frontend. The key privacy requirement is that plaintext numeric policy rules must not remain in the final confidential execution path.

Primary source of truth:

- PRD: `docs/prd.md`
- Local issues: `docs/issues/*.md`
- Agent prompt: `docs/PROMPT.md`

## Product Direction

**Polet AI Confidential Solana Control Layer for AI Agents**

Users configure confidential numeric policy rules, grant an AI agent a temporary session key, and let the agent request guarded strategy intents. The current Solana path uses Jupiter for token intelligence, pricing, and route/build previews. The new Ika path should use Polet as the Solana policy/control layer: the contract enforces guardrails on-chain and, only after approval, CPI-calls Ika Pre-Alpha `approve_message` so a dWallet can sign a bridgeless destination-chain trading intent.

Core demo:

- DCA run: 5 USDC -> SOL
- Confidential max per run: 10 USDC
- Confidential daily cap: 20 USDC
- Block scenario: 25 USDC
- Allow scenario: 5 USDC
- Ika primary destination: Sui/SUI
- Ika optional destination: Ethereum/ETH
- Ika claim boundary: pre-alpha signed bridgeless trading intent, not production MPC or executed asset settlement

Track alignment:

- Encrypt: confidential numeric policy enforcement is core.
- Ika: dWallet bridgeless signing rail controlled by Polet's Solana program after guardrail approval.
- Jupiter: Tokens + Price + Recurring compatibility + Swap V2 `/build` fallback.
- National Campus Hackathon Indonesia: Consumer App / DeFi safety for Indonesian users.

## In Progress

- None.

## Blocked

- GitHub Issues mirror: local issue specs exist in `docs/issues/`, but they have not yet been published to GitHub Issues.
- Deleted legacy docs remain deleted in the working tree: `docs/GRILL-SESSION.md`, `docs/PRD-PRODUCTION.md`, `docs/PRD.md`.

## Next Issues

### Critical Path

- `046-frontend-ethereum-ika-optional-route-ui` — Add the optional Ethereum/ETH Ika approved route as a first-class frontend demo path instead of using it as an unsupported route.
- `048-frontend-official-encrypt-status-surface` — Add frontend pending/verified Encrypt lifecycle states once the official Encrypt issue 041 proxy lifecycle is available.

### Stretch

- `051-encrypt-test-harness-compatibility` — Add full `encrypt-solana-test` / `EncryptTestContext::process_pending()` executor coverage once the Encrypt harness dependency graph can be pinned or patched to match this workspace's Solana/Anchor versions.
- `043-frontend-recovery-authority-ui` — Add recovery authority and recover-access UI for compromised sessions and staged dWallet controller migration.
- `044-frontend-passkey-coapproval-demo-ui` — Add passkey co-approval prototype UI as UX helper for shared Ika approvals.
- `045-frontend-ika-destination-broadcast-ui` — Add disabled-by-default destination memo broadcast demo UI for eligible Ika pre-alpha signatures.
- `047-frontend-route-risk-guardrail-controls` — Add frontend route-risk controls for slippage, price impact, liquidity, and verified route policy.
- `049-frontend-demo-command-center-polish` — Consolidate all frontend demo features into one command center after the prerequisite slices land.

### Frontend Demo Scope

- Do not expose legacy public policy routes in the primary frontend demo. Legacy routes remain prior-foundation code only.
- Frontend demo features should use safe transaction summaries, explicit user signing, devnet/localnet boundaries, and honest pre-alpha disclaimers.

## Dependency Graph

```text
001-confidential-smart-wallet-core
├── 002-confidential-numeric-policy-enforcement
│   └── 005-confidential-dca-execution-path
├── 003-pda-token-custody-usdc-sol-dca
│   └── 005-confidential-dca-execution-path
└── 005-confidential-dca-execution-path

004-jupiter-strategy-gateway
└── 005-confidential-dca-execution-path

005-confidential-dca-execution-path
├── 006-agent-sdk-strategy-intents
├── 007-consumer-demo-frontend
└── 008-hackathon-docs-and-dx-report

006-agent-sdk-strategy-intents
└── 009-real-agent-runtime-integration

009-real-agent-runtime-integration
└── 010-multichain-agentic-wallet-intents
    └── 011-ika-bridgeless-execution-request
        └── 012-hybrid-agent-demo-jupiter-ika-encrypt

012-hybrid-agent-demo-jupiter-ika-encrypt
├── 008-hackathon-docs-and-dx-report
└── 018-agent-trade-sdk-adapters
    └── 019-ika-prealpha-policy-gated-signing
        └── 020-destination-chain-broadcast-demo

013-smart-wallet-identity-module
└── 014-confidential-numeric-policy-module
    ├── 015-legacy-policy-path-quarantine
    ├── 041-official-encrypt-policy-graph-execution
    │   ├── 050-contract-official-encrypt-verified-ika-cpi-lifecycle
    │   └── 051-encrypt-test-harness-compatibility
    └── 016-proxy-execution-module
        └── 017-execution-payload-module

007-consumer-demo-frontend
└── 021-consumer-frontend-navigation-simplification
    ├── 022-linear-confidential-dca-demo-checklist
    ├── 023-quiet-fintech-frontend-visual-refresh
    └── 024-consumer-frontend-e2e-demo-coverage
        ├── 042-frontend-shared-ika-approval-ui
        │   ├── 043-frontend-recovery-authority-ui
        │   └── 044-frontend-passkey-coapproval-demo-ui
        ├── 046-frontend-ethereum-ika-optional-route-ui
        │   ├── 045-frontend-ika-destination-broadcast-ui
        │   └── 047-frontend-route-risk-guardrail-controls
        ├── 048-frontend-official-encrypt-status-surface
        └── 049-frontend-demo-command-center-polish

025-official-ika-dwallet-prealpha-alignment
├── 026-canonical-bridgeless-order-message
│   └── 027-polet-contract-ika-approve-message-cpi
│       └── 028-proxy-ika-dwallet-transaction-builder
│           └── 029-agent-sdk-ika-sui-signed-intent
│               ├── 030-control-layer-frontend-ika-dwallet-demo
│               │   ├── 032-control-layer-readme-video-narrative
│               │   ├── 035-shared-access-and-multisig-lite-ika-approvals
│               │   │   ├── 036-passkey-owner-session-auth
│               │   │   └── 037-recovery-authority-and-dwallet-controller-rotation
│               │   └── 038-chain-and-asset-allowlist-guardrails
│               │       └── 039-bridgeless-slippage-and-risk-guardrails
│               └── 031-ika-devnet-smoke-and-messageapproval-verification
│                   ├── 040-official-ika-message-hash-and-signing-lifecycle
│                   ├── 032-control-layer-readme-video-narrative
│                   ├── 033-sui-transaction-digest-adapter
│                   └── 034-ethereum-transaction-digest-adapter
```

## Done

### Current Roadmap

- [x] `001-confidential-smart-wallet-core` — Refactored the single contract core into a PDA wallet with owner authority, proxy metadata, policy commitment, Merkle root metadata, policy sequence, kill-switch revocation slot, and session-key records. Removed plaintext `daily_limit`, `daily_spent`, and serialized plaintext policy fields from the contract state and final session execution path. Synced the proxy IDL to the new contract ABI. Added tests for initialization, policy/proxy metadata, grant, revoke, revoke-all, expired/missing/revoked sessions, valid session execution, and stale policy sequence.
  - Verification: `NO_DNA=1 cargo build`, `NO_DNA=1 anchor build`, and `NO_DNA=1 cargo test` all pass in `contract/`.
- [x] `002-confidential-numeric-policy-enforcement` — Added the first pre-alpha confidential numeric policy path to the single contract. Wallet state now stores masked numeric policy values plus a commitment and witness hash instead of plaintext max-per-run, daily-cap, or daily-spent fields. Added owner policy setup and session execution gated by max-per-run, daily-cap, daily-spent update, and UTC-day reset behavior. Synced the proxy IDL to the new contract ABI.
  - Verification: `NO_DNA=1 cargo build`, `NO_DNA=1 anchor build`, and `NO_DNA=1 cargo test` all pass in `contract/`; `bun test` passes in `proxy/`.
  - Note: this is an Encrypt pre-alpha interface simulation using masked values and a witness hash. It does not claim production-grade confidentiality until real Encrypt primitives replace the witness path.
- [x] `003-pda-token-custody-usdc-sol-dca` — Added PDA-owned demo token custody metadata to the single wallet state plus an owner-only `register_demo_custody` instruction. The contract records USDC mint/account and SOL/wSOL mint/account references only after validating that the supplied SPL Token accounts are owned by the wallet PDA authority, so deposit addresses can be surfaced by proxy/frontend without putting funds under direct owner authority. Synced the proxy IDL to the new ABI.
  - Verification: `NO_DNA=1 anchor build` and `NO_DNA=1 cargo test` pass in `contract/`; `bun test` passes in `proxy/`.
  - Note: this slice establishes custody references and validation. Token movement from the PDA-owned accounts remains intentionally deferred to the Jupiter/confidential execution slices.
- [x] `004-jupiter-strategy-gateway` — Added an isolated proxy Jupiter strategy gateway for Tokens V2 metadata/quality fields, Price V3 USDC/SOL pricing, Recurring compatibility recording, and Swap API V2 `/build` fallback with the Polet wallet as taker. The gateway validates `JUPITER_API_KEY` before requests, supports injected fetch for deterministic tests, and captures Jupiter DX notes for the final report.
  - Verification: `bun test` and `bun run build` pass in `proxy/`.
  - Note: Recurring is recorded as incompatible for the current MVP path because Polet still needs every DCA run to pass confidential PDA policy gating immediately before spending; Swap V2 `/build` remains the execution fallback.
- [x] `005-confidential-dca-execution-path` — Added the proxy confidential DCA run path at `POST /intent/dca/run`, connecting wallet/session validation, Jupiter Tokens/Price/Swap V2 prechecks, pre-alpha confidential max-per-run and daily-cap witness evaluation, non-leaking block responses, and unsigned confidential session transaction construction. The allowed path uses the smart wallet PDA as Jupiter taker/payer and Polet contract wallet authority; the proxy does not sign or broadcast.
  - Verification: `bun test` and `bun run build` pass in `proxy/`.
  - Note: this remains an Encrypt pre-alpha masked witness demo path, not a production confidentiality guarantee. Jupiter Recurring remains incompatible for the MVP, so Swap V2 `/build` fallback is used.
- [x] `006-agent-sdk-strategy-intents` — Extended the SDK with `createDcaIntent`, `createRiskGatedSwapIntent`, proxy `submitIntent` and `evaluateIntentWithProxy` helpers, Jupiter USDC/SOL constants, typed proxy errors, and OpenClaw/Hermes-style agent examples. Existing transfer, swap, stake, unstake, delegate, undelegate, and custom builders remain available.
  - Verification: `bun test` and `bun run build` pass in `sdk/`.
  - Note: DCA submission targets the issue 005 proxy route `POST /intent/dca/run`; risk-gated swaps intentionally retain action `swap` for backward compatibility with the existing proxy evaluator.
- [x] `007-consumer-demo-frontend` — Replaced the legacy transfer simulator with a user-facing confidential DCA demo wired to real proxy/contract paths. The frontend now presents Polet as a confidential USDC -> SOL DCA smart wallet, builds owner-signed confidential policy and PDA custody setup transactions through the proxy, calls `/intent/dca/run` for 25 USDC blocked and 5 USDC allowed runs, keeps activity logs free of private thresholds, supports Indonesian/English copy, and shows Jupiter Swap V2 route/build details for approved runs.
  - Verification: `bun run test src/components/DemoTab.test.tsx` and `bun run build` pass in `frontend/`; `bun test ./tests/confidential-dca-execution.test.ts ./tests/transaction-builder.test.ts` passes in `proxy/`.
  - Note: the current devnet demo proves policy gating and Jupiter route/build integration, but does not execute a mainnet Jupiter swap. Production-like swap execution requires a follow-up PDA signing/CPI path or a separate owner-signed mainnet flow.
- [x] `008-hackathon-docs-and-dx-report` — Added the root README, Polet-specific frontend README, Jupiter DX report, and allow/block demo script. The docs describe target users, problem/solution, demo flow, devnet program/proxy/frontend URLs, build/test/run commands, Encrypt pre-alpha limitations, Jupiter Recurring vs Swap V2 `/build` decision, and Ika as a future target rail without claiming verified settlement.
  - Verification: documentation-only change; no code tests were required.
  - Note: issue 012 remains future hybrid implementation work. The docs intentionally frame Ika as a target boundary, not a completed production execution rail.
- [x] `009-real-agent-runtime-integration` — Added a local scripted agent runtime in the SDK that creates USDC -> SOL DCA intents with the existing SDK builder, submits them to the Polet proxy, and reports allowed/blocked decisions for 5 USDC and 25 USDC scenarios. Added a CLI runner and setup docs as the base adapter for future multichain/Ika intent slices.
  - Verification: `bun test` and `bun run build` pass in `sdk/`.
  - Note: the runtime does not sign or broadcast transactions. It submits agent intents to the proxy and reports the policy result plus any unsigned smart-wallet execution payload returned by the proxy.
- [x] `010-multichain-agentic-wallet-intents` — Added a multichain strategy intent envelope with source chain/asset, target chain/asset, amount, execution rail, and confidential policy witness. The SDK now exposes `createMultichainStrategyIntent`, the proxy accepts `POST /intent/multichain/run`, and the current supported executable combination maps Solana USDC -> SOL on the Jupiter rail into the existing confidential DCA route/build preview path. The frontend strategy panel now shows the multichain boundary while explicitly noting that Ika settlement is not executed in this slice.
  - Verification: `bun test` and `bun run build` pass in `sdk/`; `bun test` and `bun run build` pass in `proxy/`; `bun run test src/components/DemoTab.test.tsx` and `bun run build` pass in `frontend/`.
  - Note: unsupported Ika/cross-chain combinations are validated and rejected for now. Issue 011 owns the bridgeless execution request shape.
- [x] `011-ika-bridgeless-execution-request` — Added the approved-only Ika bridgeless request path for multichain intents. The proxy now branches `executionRail: "ika"` into confidential session/policy evaluation and returns an `executionRail: "ika-bridgeless"` envelope with source/target assets, amount, owner, session key, smart wallet authority, policy sequence, non-leaking approved status, and explicit non-settlement boundary. Blocked or stale sessions suppress the request envelope. The frontend can request and display the bridgeless route boundary without exposing private thresholds.
  - Verification: `bun test` and `bun run build` pass in `proxy/`; `bun test` and `bun run build` pass in `sdk/`; `bun run test` and `bun run build` pass in `frontend/`.
  - Note: this prepares an Ika-oriented request envelope only. It does not claim verified Ika settlement or real bridgeless trade execution.
- [x] `012-hybrid-agent-demo-jupiter-ika-encrypt` — Added the final scripted hybrid agent path in the SDK. The local runtime now supports `allow`, `block`, `ika`, and `hybrid` scenarios; `hybrid` runs the blocked 25 USDC DCA, approved 5 USDC Jupiter route/build DCA, and approved 5 USDC Ika bridgeless request sequence through the same proxy submission helpers. Updated the agent runtime docs, demo script, and issue notes to state what is executed, previewed, and still pre-alpha/future integration.
  - Verification: `bun run test` and `bun run build` pass in `sdk/`; `bun run test src/components/DemoTab.test.tsx` passes in `frontend/`.
  - Note: the hybrid path does not sign or broadcast transactions. Jupiter remains a route/build preview with unsigned policy-gated payload, and Ika remains a prepared request envelope without verified settlement.
- [x] `013-smart-wallet-identity-module` — Added a shared proxy smart wallet identity module for program id, wallet seed, and wallet PDA derivation, then updated proxy wallet/session/setup/execution call sites to use it. Fixed the SDK wallet PDA helper to use the contract seed `polet_wallet` and real Solana PDA derivation instead of the old `polet_ai_wallet` hash simulation.
  - Verification: `bun run test` and `bun run build` pass in `sdk/`; `bun run test` and `bun run build` pass in `proxy/`.
  - Note: this is an architecture hardening slice from `/improve-codebase-architecture`; it reduces duplicated wallet identity knowledge before deeper confidential policy refactors.
- [x] `014-confidential-numeric-policy-module` — Added a shared proxy confidential numeric policy module for USDC amount parsing, masked witness hashing/encoding, current day indexing, setup payload construction, and safe allow/block evaluation. DCA and Ika guarded execution now use that interface, and wallet setup no longer owns the private masked-value construction. Added a contract-side `confidential_policy` module that localizes witness verification, limit checks, and encrypted daily-spend mutation away from the main program handlers.
  - Verification: `bun test` and `bun run build` pass in `proxy/`; `NO_DNA=1 cargo build`, `NO_DNA=1 anchor build`, and `NO_DNA=1 cargo test` pass in `contract/`.
  - Note: this remains an Encrypt pre-alpha masked witness simulation boundary, not a production confidentiality claim.
- [x] `015-legacy-policy-path-quarantine` — Quarantined the public plaintext policy foundation behind explicit legacy names and routes. Legacy allowlist/blocklist/maxAmount evaluation now lives in `legacy-public-policy-engine`, legacy intent evaluation/execution is under `/legacy/intent/*`, legacy public templates are under `/legacy/template/*`, and public wallet policy setup is under `/wallet/legacy/set-policy`. The current product routes remain `/wallet/set-confidential-policy`, `/intent/dca/run`, and `/intent/multichain/run`.
  - Verification: `bun test` and `bun run build` pass in `proxy/`; `bun test` and `bun run build` pass in `sdk/`; `bun run test src/components/DemoTab.test.tsx` and `bun run build` pass in `frontend/`; `NO_DNA=1 cargo build` passes in `contract/`.
  - Note: legacy tests remain as prior-foundation coverage and are labeled as legacy; confidential DCA tests do not depend on plaintext public policy helpers.
- [x] `016-proxy-execution-module` — Added a shared proxy strategy execution interface for wallet lookup, session validation, optional custody checks, strategy preparation, confidential policy evaluation, and allowed payload construction. Confidential DCA now supplies Jupiter route/build preparation and transaction construction as adapter steps; Ika bridgeless requests reuse the same guardrail decision shape while preserving their existing envelope.
  - Verification: `bun test ./tests/strategy-execution.test.ts ./tests/confidential-dca-execution.test.ts ./tests/ika-bridgeless-request.test.ts` and `bun run build` pass in `proxy/`.
  - Note: blocked responses remain non-leaking, and allowed DCA/Ika response shapes remain externally unchanged.
- [x] `017-execution-payload-module` — Added named proxy and contract execution payload modules for the confidential transfer instruction contract. The proxy now centralizes transfer intent layout constants, discriminator bytes, u32/u64 encoding, witness validation, and account metas; the contract now centralizes transfer intent parsing, instruction validation, destination validation, and amount decoding.
  - Verification: `bun test` and `bun run build` pass in `proxy/`; `NO_DNA=1 cargo fmt --check`, `NO_DNA=1 cargo test`, and `NO_DNA=1 anchor build` pass in `contract/`.
  - Note: the confidential DCA allowed path still returns an unsigned transaction with `sessionKey` as the required signer.
- [x] `018-agent-trade-sdk-adapters` — Added the high-level SDK `createPoletAgent()` entry point with `trade()` adapters for simple Solana USDC -> SOL Jupiter DCA and explicit Ika multichain request inputs. Results now normalize Jupiter approval to `preview-ready` / `not-executed`, Ika approval to `request-prepared` / `not-executed`, blocked decisions to non-leaking `blocked`, and unsupported routes to `not-supported`.
  - Verification: `bun test` and `bun run build` pass in `sdk/`.
  - Note: this is an SDK control-layer API over existing proxy routes. It does not execute Ika settlement or claim production private trading.
- [x] `019-ika-prealpha-policy-gated-signing` — Added a proxy Ika Solana Pre-Alpha signing proof model behind the existing guarded Ika rail. Policy-approved Ika intents derive a deterministic dWallet account, CPI authority PDA, message approval PDA, message hash, signature scheme, and devnet/mock-signer environment metadata. Issue 040 superseded the original unsigned status wording: prepared unsigned approval transactions now return `status: "approval-transaction-prepared"` with `settlement: "not-executed"`. Policy-blocked intents still suppress all Ika approval data.
  - Verification: `bun test` and `bun run build` pass in `proxy/`; `bun test` and `bun run build` pass in `sdk/`.
  - Note: this is an Ika Pre-Alpha control-layer proof only. It does not broadcast an `approve_message` transaction, execute final settlement, or claim production MPC security.
- [x] `020-destination-chain-broadcast-demo` — Selected a narrow Solana devnet memo-proof destination broadcast path for Ika Pre-Alpha results. The proxy can construct a memo transaction from a `signature-produced-prealpha` result, keeps broadcast disabled by default, only submits when explicit demo env configuration is enabled, and returns a devnet transaction receipt/explorer URL when submitted. The SDK now exposes `broadcastIkaDestinationDemo()`.
  - Verification: `bun test ./tests/destination-broadcast-demo.test.ts` and `bun run build` pass in `proxy/`; `bun run test` and `bun run build` pass in `sdk/`.
  - Note: this is a no-asset Solana devnet memo receipt proof only. It does not claim production Ika settlement, production MPC, or arbitrary destination-chain support.
- [x] `021-consumer-frontend-navigation-simplification` — Simplified the connected-wallet consumer navigation to `DCA Demo` and `Agent Access`, made the DCA demo the default view, removed the dashboard legacy public policy configurator path, and folded the static overview cards into a compact DCA workflow status summary.
  - Verification: `bun run test src/components/WalletDashboard.test.tsx src/components/DemoTab.test.tsx` and `bun run build` pass in `frontend/`.
  - Note: legacy public policy helpers still exist as quarantined prior-foundation code, but they are no longer linked or invoked from the primary consumer dashboard.
- [x] `022-linear-confidential-dca-demo-checklist` — Added a localized live checklist to the confidential DCA demo for wallet initialization, PDA custody, agent selection, confidential policy save, USDC -> SOL strategy readiness, deliberate 25 USDC block, 5 USDC allow, and safe activity log review. Primary CTAs now unlock according to that linear path, with the allowed Jupiter preview run gated behind the blocked scenario.
  - Verification: `bun run test src/components/DemoTab.test.tsx src/components/WalletDashboard.test.tsx` and `bun run build` pass in `frontend/`.
  - Note: allowed Jupiter output remains explicitly framed as route/build preview plus unsigned transaction boundary; no frontend swap execution is claimed.
- [x] `023-quiet-fintech-frontend-visual-refresh` — Refreshed the consumer frontend into a quieter fintech dashboard: the homepage first viewport is now an operational DCA control panel instead of a large marketing hero, decorative radial/orb treatments were removed, the palette moved to neutral surfaces with restrained blue primary actions, and cards/panels use compact dashboard radii and shadows.
  - Verification: `bun run test src/routes/-index.test.tsx src/components/DemoTab.test.tsx src/components/WalletDashboard.test.tsx` and `bun run build` pass in `frontend/`.
  - Note: state colors remain reserved for success, blocked, warning, and error communication.
- [x] `024-consumer-frontend-e2e-demo-coverage` — Added Playwright E2E coverage for the redesigned consumer DCA workflow. The suite runs desktop and mobile Chromium projects against a local Vite server and deterministic `/e2e/consumer-demo` harness, covering simplified navigation, first viewport checklist, CTA gating, policy redaction, 25 USDC blocked privacy, 5 USDC Jupiter preview/unsigned payload boundary, and responsive readability.
  - Verification: `bun run test:e2e`, `bun run test src/routes/-index.test.tsx src/components/DemoTab.test.tsx src/components/WalletDashboard.test.tsx`, and `bun run build` pass in `frontend/`.
  - Note: Playwright browser binaries are external developer-machine tooling; install with `bunx playwright install chromium` when setting up a fresh environment.
- [x] `025-official-ika-dwallet-prealpha-alignment` — Added the official Ika Solana Pre-Alpha integration memo with checked date, docs/repo links, devnet program id, gRPC/RPC endpoints, SDK/CPI crate references, dWallet creation/import notes, Polet CPI authority transfer flow, `approve_message` account/argument shape, MessageApproval read path, Sui-primary and Ethereum-optional smoke plans, and mock-signer/pre-alpha limitations. Updated README, demo script, agent runtime docs, and issue notes so the current local Ika metadata is not described as production MPC, verified settlement, or official on-chain MessageApproval execution.
  - Verification: documentation-only change; no code tests were required.
  - Note: issue 027 must replace the local proxy compatibility derivation with the official program id `87W54kGYFQ1rgWqMeu4XTPHWXWmXSQCcjm8vCTfiq1oY`, Polet CPI authority seed `__ika_cpi_authority`, and official MessageApproval PDA/account helpers.
- [x] `026-canonical-bridgeless-order-message` — Added canonical `polet.bridgeless.order.v1` modules in SDK and proxy for deterministic Ika dWallet order messages. The payload includes intent id, Solana USDC source, Sui-primary/Ethereum-optional target, source USDC-equivalent policy amount, slippage, owner, session key, policy sequence, nonce, and expiry. The proxy now attaches `canonicalOrder` and `canonicalOrderHash` to approved Ika envelopes only, and docs show the canonical JSON payload plus stable digest.
  - Verification: `bun test ./tests/bridgeless-order.test.ts ./tests/ika-bridgeless-request.test.ts` and `bun run build` pass in `proxy/`; `bun test ./tests/intent-builder.test.ts` and `bun run build` pass in `sdk/`.
  - Note: issue 027 should use `canonicalOrderHash` as the message hash input for the official Ika `approve_message` CPI after re-deriving accounts with the official Pre-Alpha SDK helpers.
- [x] `027-polet-contract-ika-approve-message-cpi` — Added Polet's on-chain `approve_ika_message_as_session` instruction. It accepts wallet/session/dWallet/MessageApproval/Ika-program accounts, canonical order hash, source amount, expiry, attestation slot/policy sequence, policy witness, user public key, signature scheme, and MessageApproval bump. The instruction validates session freshness, order expiry, and confidential numeric policy before signing an Ika `approve_message` CPI with Polet's `__ika_cpi_authority` PDA. Added a deterministic `mock_ika` Anchor program for CI and tests that prove allowed 5-unit requests call CPI while blocked 25-unit, stale, and expired requests do not.
  - Verification: `NO_DNA=1 cargo fmt --check`, `NO_DNA=1 anchor build`, and `NO_DNA=1 cargo test` pass in `contract/`; `bun test` and `bun run build` pass in `proxy/`.
  - Note: CI uses the mock Ika program. Live devnet smoke verification is documented in `docs/ika-devnet-smoke-runbook.md`. This is not production MPC or settlement.
- [x] `028-proxy-ika-dwallet-transaction-builder` — Updated the proxy Ika rail to build the unsigned Polet `approve_ika_message_as_session` transaction for allowed Solana USDC -> Sui SUI intents. Allowed responses now include canonical order hash, dWallet, MessageApproval, signature scheme, destination metadata, Pre-Alpha boundary, and `poletApprovalTransaction` for the session signer. Blocked/stale paths still suppress Ika approval data and private thresholds. The proxy uses the pinned official Ika devnet program id and Polet `__ika_cpi_authority` PDA seed, but does not sign, simulate, send, or claim settlement.
  - Verification: `bun test` and `bun run build` pass in `proxy/`.
  - Note: live devnet smoke verification and MessageApproval inspection are documented in `docs/ika-devnet-smoke-runbook.md`.
- [x] `029-agent-sdk-ika-sui-signed-intent` — Updated the SDK high-level agent API for Sui-primary Ika signed-intent approvals. `createPoletAgent().trade({ rail: "ika", to: { chain: "sui", asset: "SUI" } })` now normalizes proxy-built Polet approval transactions into `details.proof` with dWallet, canonical order hash, message hash, MessageApproval, CPI authority, signature scheme, destination, optional devnet smoke proof, and non-settlement boundary. Unsupported Ika destinations are rejected before proxy submission, expired orders normalize to blocked, and returned high-level intents redact confidential witness bytes. The local runner now supports `ika-sui` plus the existing hybrid flow.
  - Verification: `bun test` and `bun run build` pass in `sdk/`.
  - Note: the SDK still does not sign or broadcast transactions; live devnet smoke verification is manual via `docs/ika-devnet-smoke-runbook.md`.
- [x] `030-control-layer-frontend-ika-dwallet-demo` — Reframed the consumer demo as a confidential Solana control layer for AI agents instead of a DCA-only wallet. The primary panel now shows Jupiter as the Solana USDC -> SOL strategy rail and Ika as the Sui/SUI dWallet rail, with Ethereum/ETH marked optional future. Added separate 25 USDC-equivalent blocked and 5 USDC-equivalent approved Ika actions; blocked Ika logs suppress dWallet approval data and private thresholds, while approved logs show an Ika approval transaction prepared proof with dWallet, MessageApproval, message hash, signature scheme, and pre-alpha non-settlement boundary.
  - Verification: `bun run test src/components/DemoTab.test.tsx` and `bun run build` pass in `frontend/`; `bunx playwright test` passes in `frontend/` against an escalated local dev server.
  - Note: the frontend still does not sign, simulate, or broadcast the Polet/Ika approval transaction. Live devnet smoke verification is manual via `docs/ika-devnet-smoke-runbook.md`.
- [x] `031-ika-devnet-smoke-and-messageapproval-verification` — Added the manual Ika Solana Pre-Alpha devnet smoke runbook. It documents required devnet accounts, faucet/funding needs, proxy/frontend/agent/Ika environment variables, dWallet DKG/import boundary, authority transfer to Polet's `__ika_cpi_authority` PDA, the 25 USDC-equivalent block path before Ika approval, the 5 USDC-equivalent Sui approval path with the real dWallet override, session-signer simulation/signing requirements, MessageApproval polling/inspection, evidence capture, failure states, and the CI/manual boundary.
  - Verification: documentation/HITL runbook change; no live devnet transaction was signed or broadcast during implementation. Deterministic local Ika coverage remains under `mock_ika`, `proxy/tests/ika-bridgeless-request.test.ts`, and `proxy/tests/transaction-builder.test.ts`.
  - Note: the devnet smoke remains optional/manual because it depends on external Ika Pre-Alpha devnet, gRPC availability, faucets, and mock signer timing. Operators must simulate and review the returned unsigned Polet approval transaction before signing with the session signer.
- [x] `032-control-layer-readme-video-narrative` — Reframed the public docs around Polet as a confidential Solana control layer for AI agents. The README now has explicit Encrypt and Ika sections, the demo script centers the required 25 USDC blocked/no-Ika-approval, 5 USDC Jupiter DCA approval, and 5 USDC Sui Ika signed-intent approval outcomes, and the agent runtime docs include an OpenClaw/Hermes-style adapter pattern with safe handling rules.
  - Verification: documentation-only change; no code tests were required.
  - Note: docs continue to avoid production MPC, production confidentiality, real bridgeless settlement, and mainnet trading claims.
- [x] `033-sui-transaction-digest-adapter` — Added the Sui-primary Ika digest adapter. Approved Solana USDC -> Sui SUI requests now keep the canonical Polet order hash as an order reference, map the approved order into a Sui devnet sign-only `zero-mist-transfer-proof` digest artifact, and expose SDK proof metadata without claiming broadcast or settlement. Issue 040 superseded hash semantics: the Sui digest is destination signing metadata, not the Ika MessageApproval lookup hash.
  - Verification: `bun test ./tests/sui-transaction-digest.test.ts ./tests/ika-bridgeless-request.test.ts` and `bun run build` pass in `proxy/`; `bun test ./tests/intent-builder.test.ts ./tests/local-agent-runtime.test.ts` and `bun run build` pass in `sdk/`; `bun run build` passes in `frontend/`.
  - Note: the Sui artifact is `broadcastable: false`, `productionSettlement: false`, and uses either a validated `nativeDestinationAccount` recipient or Polet's fixed devnet verifier address. Policy-blocked responses still suppress all Ika/Sui digest proof fields.
- [x] `034-ethereum-transaction-digest-adapter` — Added the optional Ethereum Ika digest adapter without changing the Sui-primary demo path. Approved Solana USDC -> Ethereum ETH requests now map the canonical Polet order into a Sepolia sign-only EIP-191 `zero-wei-transfer-proof` digest and expose SDK proof metadata without claiming broadcast or settlement. Issue 040 superseded hash semantics: the Ethereum digest is destination signing metadata, not the Ika MessageApproval lookup hash.
  - Verification: `bun test ./tests/ethereum-transaction-digest.test.ts ./tests/sui-transaction-digest.test.ts ./tests/ika-bridgeless-request.test.ts` and `bun run build` pass in `proxy/`; `bun test ./tests/intent-builder.test.ts ./tests/local-agent-runtime.test.ts` and `bun run build` pass in `sdk/`; `bun run build` passes in `frontend/`.
  - Note: the Ethereum artifact is `broadcastable: false`, `productionSettlement: false`, and uses either a validated `nativeDestinationAccount` recipient or Polet's fixed Sepolia verifier address. Policy-blocked responses still suppress all Ika/Sui/Ethereum digest proof fields.
- [x] `035-shared-access-and-multisig-lite-ika-approvals` — Added shared-access M-of-N approval for Ika requests across proxy, SDK, frontend activity state, and contract enforcement. Ika intents can carry `sharedAccess`, proxy verifies deterministic co-approver challenge signatures and returns non-leaking `needs-approval` progress when quorum is missing, owner-signed proxy routes can configure/revoke shared Ika approvers on-chain, and `approve_ika_message_as_session` now requires enough authorized co-approver signer remaining accounts before confidential spend mutation or Ika CPI.
  - Verification: `NO_DNA=1 cargo build-sbf` and `NO_DNA=1 cargo test` pass in `contract/`; `bun test` and `bun run build` pass in `proxy/`; `bun test` and `bun run build` pass in `sdk/`; `bun run test src/components/DemoTab.test.tsx` passes in `frontend/` with a Vitest/Vite shutdown warning after tests complete; `bun run build` passes in `frontend/`.
  - Note: contract enforcement uses Solana signer co-approver accounts for the on-chain boundary. The off-chain challenge signature flow remains the agent-runtime collection UX; no production MPC or settlement is claimed.
- [x] `036-passkey-owner-session-auth` — Chose passkeys for shared Ika co-approval UX only and added a proxy WebAuthn-style prototype. The new passkey challenge binds owner, session key, shared Ika approval challenge, credential id, rpId, and expiry; verification checks origin, rpId hash, challenge, user presence, optional user verification, and P-256 signature while explicitly preserving Solana owner/session/co-approver signatures and on-chain policy checks as the authority boundary.
  - Verification: `bun test ./tests/passkey-coapproval.test.ts` and `bun run build` pass in `proxy/`.
  - Note: passkeys are not the critical path for the MVP and do not control Ika dWallet authority, Polet wallet funds, or contract quorum by themselves.
- [x] `037-recovery-authority-and-dwallet-controller-rotation` — Added owner-configured recovery authority support plus a guarded recovery instruction that can revoke compromised sessions, advance the global revocation slot, rotate shared Ika co-approver quorum metadata, and stage pending dWallet controller migration metadata without touching confidential numeric policy values. Added proxy transaction builders for recovery authority setup and access recovery with redacted activity states.
  - Verification: `NO_DNA=1 cargo fmt --check`, `NO_DNA=1 anchor build`, and `NO_DNA=1 cargo test` pass in `contract/`; `bun test` and `bun run build` pass in `proxy/`.
  - Note: the dWallet controller migration is staged metadata only. It does not execute live Ika authority transfer, settlement, or production MPC, and older initialized wallet accounts may need migration/reinitialization because the account layout grew.
- [x] `038-chain-and-asset-allowlist-guardrails` — Added explicit public route guardrails for Ika multichain intents. The proxy now evaluates allowed source chains, target chains, source assets, and target assets before confidential numeric policy execution or Ika approval construction. Sui/SUI remains allowed for the primary demo path, optional Ethereum/ETH remains supported by the default route policy, and unsupported routes return safe `IKA_ROUTE_NOT_ALLOWED` blocks with no policy thresholds, witness bytes, dWallet data, or MessageApproval data. SDK and frontend now pass/display route guardrails and safe unsupported-route explanations.
  - Verification: `bun test` and `bun run build` pass in `proxy/`; `bun test` and `bun run build` pass in `sdk/`; `bun run test src/components/DemoTab.test.tsx` and `bun run build` pass in `frontend/`.
  - Note: this is an explicit public allowlist model, not encrypted allowlist membership. Confidential numeric limits remain enforced separately after route guardrails pass.
- [x] `039-bridgeless-slippage-and-risk-guardrails` — Added route-risk guardrails for Ika bridgeless orders. Canonical orders now bind optional route-risk metadata, the proxy validates slippage/price-impact/liquidity/verified-route constraints before confidential policy mutation or Ika approval construction, the SDK preserves the fields through builders and the agent trade adapter, and the frontend displays a safe route-risk status without private policy limits.
  - Verification: `bun test` and `bun run build` pass in `proxy/`; `bun test` and `bun run build` pass in `sdk/`; `bun run test src/components/DemoTab.test.tsx` and `bun run build` pass in `frontend/`.
  - Note: risk metadata is an agent/proxy precheck signal in this slice. It is bound into the canonical signed order when present, but it does not prove real destination-chain settlement or production market execution.
- [x] `040-official-ika-message-hash-and-signing-lifecycle` — Aligned Polet's Ika approval path with official Pre-Alpha hash semantics. Proxy responses now separate `canonicalOrderHash`, Keccak-256 `ikaMessageHash`, and destination signing digests; unsigned approval transactions use `approval-transaction-prepared` rather than `message-approved`; the transaction builder and contract argument names now pass `ika_message_hash` into Ika `approve_message`; SDK/frontend/docs use the new lifecycle wording.
  - Verification: `bun test ./tests/ika-bridgeless-request.test.ts ./tests/transaction-builder.test.ts` and `bun run build` pass in `proxy/`; `bun test ./tests/intent-builder.test.ts ./tests/local-agent-runtime.test.ts` and `bun run build` pass in `sdk/`; `bun run test src/components/DemoTab.test.tsx` and `bun run build` pass in `frontend/`; `NO_DNA=1 cargo test ika_approval` passes in `contract/`.
  - Note: `NO_DNA=1 anchor build` compiled both programs but exited with `IDL doesn't exist` during Anchor's IDL step. The checked-in proxy IDL argument name was updated manually.
- [x] `041-official-encrypt-policy-graph-execution` — Completed the official Encrypt policy graph boundary. Wallet policy state stores official Encrypt ciphertext identifiers and pending output slots; `set_official_encrypt_ciphertext_policy` accepts real Encrypt-owned policy ciphertext accounts through the devnet/pre-alpha `EncryptContext` account surface; `execute_encrypt_policy_graph_as_session` submits `polet_policy_guardrail_graph`; proxy/SDK/frontend status surfaces distinguish pending, verified allowed, and verified blocked states without leaking private policy data.
  - Verification: `NO_DNA=1 anchor build`, `NO_DNA=1 cargo fmt --check`, and `NO_DNA=1 cargo test -p contract` pass in `contract/`; `bun test` and `bun run build` pass in `proxy/`; earlier verification for SDK/frontend lifecycle types remains recorded in the issue.
  - Note: LiteSVM contract tests cover owner setup, pending graph execution state, verified allowed/blocked consumption, stale sequence/session checks, Ika CPI suppression, and masked-witness quarantine. Full `encrypt-solana-test` / `EncryptTestContext::process_pending()` executor coverage is deferred to a compatibility follow-up because the current Encrypt harness resolves an incompatible Solana 4 beta/rc dependency graph for this workspace.
- [x] `042-frontend-shared-ika-approval-ui` — Added the frontend shared Ika approval workflow. The command-center demo can build and owner-sign shared approver configuration transactions, revoke configured approvers, hydrate configured quorum state from the wallet endpoint, pass configured shared-access policy and optional collected co-approval proofs into Ika requests, and show missing/ready quorum progress without private policy thresholds or witness values.
  - Verification: `bun run test src/components/DemoTab.test.tsx` passes in `frontend/` with the existing Vitest/Vite `module is not defined` and shutdown-timeout warnings; `bun run build` passes in `frontend/`.
  - Note: co-approval proof collection remains a JSON demo input. The authority boundary remains Solana signer co-approver accounts enforced by Polet; this slice does not make passkeys or frontend proof entry direct authority over wallet funds, dWallet authority, policy execution, or settlement.
- [x] `050-contract-official-encrypt-verified-ika-cpi-lifecycle` — Added the contract-side verified Encrypt consume path for Ika approval. `approve_ika_message_with_verified_encrypt_as_session` requires the pending official Encrypt output ciphertexts, verifies the allowed decryption request against the current ciphertext digest, rejects pending or verified-blocked outputs before Ika CPI, updates the official daily-spent ciphertext pointer only on verified allowed output, and preserves session, expiry, policy sequence, canonical Ika hash, dWallet/MessageApproval, and shared approver quorum checks. The masked-witness Ika instruction now rejects when an official Encrypt ciphertext policy is configured. Synced the proxy IDL to expose the new contract instruction and errors.
  - Verification: `NO_DNA=1 anchor build`, `NO_DNA=1 cargo fmt --check`, `NO_DNA=1 cargo test -p contract --test ika_approval`, and `NO_DNA=1 cargo test` pass in `contract/`; `bun run build` passes in `proxy/`.
  - Note: tests cover the consume lifecycle with deterministic mock ciphertext/decryption request accounts. No production privacy, MPC, settlement, or broadcast claims were added.

### Planning

- [x] PRD created at `docs/prd.md`.
- [x] Local issue specs created under `docs/issues/`.
- [x] Ralph-style prompt updated at `docs/PROMPT.md`.
- [x] Progress tracker redirected to the confidential Solana control-layer roadmap.

### Previous Foundation

These completed items are prior foundation, not necessarily final architecture. Reuse concepts where useful, but refactor freely when they conflict with the final confidential product.

- [x] Wallet PDA + owner + policy hash storage.
- [x] Public allow/block policy enforcement.
- [x] Public on-chain daily spend tracking.
- [x] Temporal key grant.
- [x] Session revoke / kill switch.
- [x] Off-chain proxy intent parsing and routing.
- [x] Off-chain transaction construction.
- [x] SDK intent JSON builder.
- [x] Policy UI templates.
- [x] Frontend wallet + temporal key flow.
- [x] End-to-end blocked demo.
- [x] Wallet state extensions: proxy key, Merkle root, policy sequence, last revoked slot.
- [x] Merkle proof verification.
- [x] Slot-based revocation and policy sequence verification.
- [x] Mock TEE attestation verification.
- [x] Wallet store integration with Solana RPC.
- [x] AES-encrypted KMS for proxy/session keys.
- [x] Wallet management endpoints.
- [x] Agent auth key registration flow.
- [x] Merkle tree builder and proof generator.
- [x] SHA-256 policy hashing.
- [x] Devnet RPC wiring.
- [x] Create wallet frontend flow.
- [x] Demo tab with real Solana addresses.
- [x] HITL devnet flow.

## Notes

- The final product uses one contract. Do not create a separate `polet_v2` program.
- Encrypt pre-alpha must be described honestly. Do not claim production confidentiality.
- Jupiter `/build` is preferred for smart wallet execution because Polet needs raw instruction control.
- Jupiter Recurring is primary only if compatible with the smart wallet flow; otherwise use Polet scheduler + Swap V2 `/build`.
- Ika is the target bridgeless execution rail for multichain native-asset trading requests. Do not claim real Ika settlement until an actual Ika backend/request path is verified.
- Encrypted allowlist/blocklist membership is out of scope for the first milestone.
